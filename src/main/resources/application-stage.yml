spring:
  config:
    activate:
      on-profile: stage
  datasource:
    url: jdbc:postgresql://dpg-csrih3ggph6c73b6ur3g-a/my_database_ujvh
    username: my_database_ujvh_user
    password: ${STAGE_SECRET}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update